# Copy to apps/ingest/.env.local and fill in. .env.local is git-ignored.

# Required for the external feed fallback source
EXTERNAL_FEED_TOKEN=
EXTERNAL_FEED_ENDPOINT=

# Optional overrides (defaults in code)

