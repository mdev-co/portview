{
  "name": "@sps/ingest",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=.env.local src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint per workspace - configure when needed'",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@sps/shared": "workspace:*",
    "xstate": "^5.31.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.3",
    "vitest": "^4.1.5"
  }
}
